The majority graph images for the 4 different voting styles
are located in the graphImages folder.

In the code and image titles, user denotes user defined preferences and weights,
rand denotes random preferences and weights, oneHigher denotes that one candidate
will always be ranked above another candidate, and sameWeightPref denotes all
agents having the same weight (randomly set) and all ranking one candidate at the
same position.